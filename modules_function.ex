defmodule FunctionModules do
  # connverting a float to string
  to_string(4.30)

  # get the value of an operating system environment variable
  env


  # returns the extension componnet of a file name
  # mime extension

  return the processes current working directory

  # convertes a string to json

  # excute a command in you os shell

end
